#undef while
/** Proof of concept, implements while-else loops in the language; Warning: it breaks do-while loops! */
#define while(condition) if (bool STDX_CONCATENATE(STDX_WHILE_ELSE_VARIABLE_, __LINE__) = (condition)) while ((STDX_CONCATENATE(STDX_WHILE_ELSE_VARIABLE_, __LINE__) && ((STDX_CONCATENATE(STDX_WHILE_ELSE_VARIABLE_, __LINE__) = false) == false)) || (condition))